<nav class="navbar navbar-expand-lg navbar-light fixed-top shadow p-3 mb-5 bg-white ">
    <a class="navbar-brand btn btn-primary text-white" href="{{ path('homepage') }}">BDTech'</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse d-flex justify-content-around" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link btn btn-primary text-white mr-2" href="{{ path('bd_list') }}" id="liste">Liste</a>
            </li>
            <li class="nav-item">
                {{ render(controller('App\\Controller\\CategoryController::listCategories')) }}
            </li>
            <li class="nav-item">
                {{ render(controller('App\\Controller\\AuthorController::listAuthors')) }}
            </li>
            <li class="nav-item">
                {{ render(controller('App\\Controller\\BdCollectionController::listBdCollections')) }}
            </li>
            {% if app.user and is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link btn btn-warning text-white mr-2" href="{{ path('bd_index') }}"
                       id="admin">Administration</a>
                </li>
            {% endif %}
        </ul>
    </div>
    <div class="d-flex justify-content-end">
        {% if app.user %}
            <a class="btn btnsuccess text-white mr-2" href="{{ path('app_logout') }}"
               id="connexion">deconnexion</a>
        {% else %}
            <a class="btn btn-success text-white mr-2" href="{{ path('app_login') }}"
               id="connexion">Connexion</a>
        {% endif %}
    </div>
</nav>