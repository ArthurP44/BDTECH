{% extends 'baseAdmin.html.twig' %}

{% block title %}Author{% endblock %}

{% block body %}
    <h1>Author</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ author.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ author.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('author_index') }}">Retour</a>

    <a href="{{ path('author_edit', {'slug': author.slug}) }}">Modifier</a>

    {{ include('author/_delete_form.html.twig') }}
{% endblock %}
