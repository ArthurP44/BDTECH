{% extends 'baseAdmin.html.twig' %}

{% block title %}Ajouter auteur{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="jumbotron">
            <h1 class="mb-4">Ajouter un auteur</h1>

            {{ include('author/_form.html.twig') }}

            <a class="btn btn-primary mt-4" href="{{ path('author_index') }}">Retour</a>
        </div>
    </div>
{% endblock %}
