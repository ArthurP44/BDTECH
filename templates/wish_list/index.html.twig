{% extends 'baseAdmin.html.twig' %}

{% block title %}WishList{% endblock %}

{% block body %}
    <div class="container-fluid mt-5">
        <div class="jumbotron">
            <a class="btn btn-outline-secondary mb-3" href="{{ path('wish_list_new') }}">Ajouter</a>
            <table class="table bg-white font-weight-lighter shadow-lg">
                <thead>
                <tr>
                    <th>Id :</th>
                    <th>Auteur :</th>
                    <th>Titre :</th>
                    <th>Détails :</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                {% for wish_list in wish_lists %}
                    <tr>
                        <td>{{ wish_list.id }}</td>
                        <td>{{ wish_list.author }}</td>
                        <td>{{ wish_list.title }}</td>
                        <td>{{ wish_list.details }}</td>
                        <td>
                            <div class="row">
                                <a class="btn btn-outline-warning mr-3" href="{{ path('wish_list_edit', {'id': wish_list.id}) }}">modifier</a>
                                {{ include('wish_list/_delete_form.html.twig') }}
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5">Pas d'envies dans la la wishlist..!</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
