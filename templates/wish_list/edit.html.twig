{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit WishList{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="jumbotron">
            <div class="col-md-8 offset-2">
                <div class="card shadow-lg p-5">
                    {{ include('wish_list/_form.html.twig', {'button_label': 'enregistrer'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
